__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.venv/
venv/
ENV/
env/
.env
.env.local
experiment_results.json

# LaTeX auxiliary files
paper/*.aux
paper/*.log
paper/*.out
paper/*.toc
paper/*.synctex.gz
paper/*.fls
paper/*.fdb_latexmk
paper/*.bbl
paper/*.blg
paper/*.run.xml
# Also cover paper subdirectories (e.g. paper/workshop/)
paper/**/*.aux
paper/**/*.log
paper/**/*.out
paper/**/*.toc
paper/**/*.synctex.gz
paper/**/*.fls
paper/**/*.fdb_latexmk
paper/**/*.bbl
paper/**/*.blg
paper/**/*.run.xml
*.egg-info/
dist/
build/
*.egg

# Scratch files
scratch/
scratch_*
baseline_pre_fix.json
scratch_canary_*.py
*.tmp

# Build and submission archives
*.zip
*.tar
*.tar.gz

# Log files (everywhere)
*.log

# Generated documentation site
site/

# Local virtual environments
.verify-pypi-venv/

# Release builds
release/

# Windows redirect artifact
nul

# IDE / agent local state (never commit)
.zcode/

# Root-level scratch markdown (auto-generated)
efficiency_table.md
experiment_results.md

# Intermediate experiment JSONs (root-level, not paper source)
ablation_results.json
blind_benchmark_results.json
correction_scaling_results.json
binary_pulsar_sensitivity.json
hybrid_seed42_results.json
misspecification_results.json
mlp_baseline_results.json
multivariable_benchmark_results.json
oracle_ablation_results.json
pysr_baseline_results.json
pysr_baseline_fair.json
pysr_baseline_generous.json
real_data_results.json
reproducibility_results.json
multi_seed_summary.json
timing_results.json
gate_telemetry.json

# Root-level non-package scripts (helper/scratch)
benchmark_optimizer.py
generate_extended_figures.py
generate_figures.py
pysr_profiles.py

# Old arXiv bundle
arxiv_bundle.tar

# Results directory (intermediate analysis)
results/

# Paper PNG duplicates (LaTeX uses PDF)
paper/figures/*.png

# GitHub community files (not needed for paper source bundle), but
# keep CI + PyPI workflows tracked — they are required for the live
# repo to run tests and publish releases. Ignore only the templates.
.github/ISSUE_TEMPLATE/
.github/PULL_REQUEST_TEMPLATE.md
CONTRIBUTING.md
CODE_OF_CONDUCT.md
CHANGELOG.md

# MkDocs documentation site source
mkdocs.yml
docs/*
!docs/adcd_extended_grammar_plan.md
!adcd_extended_grammar_plan.md

# Release notes (local working copies, not paper source)
release_v*.md
zenodo_v*.md
RELEASE_NOTES*.md

# Internal strategy/submission notes
ARXIV_ENDORSERS_AND_SUBMISSION.md
PAPER_POSITIONING_STRATEGY.md

# Internal planning/upgrade/writing plans (AI-assisted, not for public history)
ADCD_Upgrade_Plan.md
ADCD_Upgrade_Plan_v2.md
ADCD_Writing_Humanization_Plan.md
ADCD_Master_Learning_Guide.md

# Zenodo/release description drafts (local only)
zenodo_v*.md
release_v*.md

# Translated documents (local learning only)
indo/

# Diagnostic results (local execution only)
ras_fase0_results.json
