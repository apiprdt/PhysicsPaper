# ADCD — fully pinned environment lockfile for reproducing paper results.
#
# This is a `pip freeze` snapshot of the environment used to generate the
# ADCD v2.2.1 benchmark results (Windows, Python 3.11). Every package is
# pinned to an exact version so that re-running the benchmarks produces
# numerically identical (modulo seed-controlled RNG) results.
#
# Reproduce with:
#     python -m venv .venv
#     .venv\Scripts\activate            # Windows
#     # source .venv/bin/activate        # macOS/Linux
#     pip install -r requirements-lock.txt
#     pip install -e .
#
# NOTE: pysr pulls in a Julia runtime on first use. The ADCD package itself
# does not require Julia; pysr is only needed to re-run the baseline
# comparison. See requirements.txt for the lighter runtime-only pin set.

black==26.5.1
contourpy==1.3.3
cycler==0.12.1
flake8==7.3.0
fonttools==4.63.0
jax==0.10.1
jaxlib==0.10.1
jaxopt==0.8.5
joblib==1.5.3
kiwisolver==1.5.0
matplotlib==3.10.9
mpmath==1.3.0
networkx==3.6.1
numpy==2.4.3
pandas==3.0.3
pillow==12.2.0
pyparsing==3.3.2
pysr==1.5.10
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0
python-dateutil==2.9.0.post0
scikit-learn==1.9.0
scipy==1.17.1
six==1.17.0
sympy==1.14.0
threadpoolctl==3.6.0
tzdata==2026.2
